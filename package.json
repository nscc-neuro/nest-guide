{
  "scripts": {
    "build": "hugo",
    "elog:init": "elog init",
    "sync:local": "elog sync -e .elog.env",
    "sync": "elog sync",
    "clean": "elog clean"
  },
  "dependencies": {
    "@elog/cli": "^0.12.2"
  }
}
